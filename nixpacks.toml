[phases.setup]
nixPkgs = ["python311", "nodejs-20_x"]

[phases.install]
cmds = [
    "npm install",
    "python3 -m pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Building DeepFish Hybrid Runtime...'"]

[start]
cmd = "npm start"
